[package]
name = "zzz-wire"
description = "KCP reassembly and generic protobuf wire handling, replacing src/kcp and the protobuf dependency"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true

[dev-dependencies]
# Only used by the protonap tests, which parse real nap.json snippets.
serde_json.workspace = true
